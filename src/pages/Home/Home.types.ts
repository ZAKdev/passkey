export interface HomeProps {
    // Add any props if needed in the future
}

export interface User {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    image: string;
}